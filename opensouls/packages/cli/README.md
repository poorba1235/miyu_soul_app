# soul-engine

The command line tool (CLI) used for interacting with the Soul Engine

## Local-only usage

- No login required; the CLI uses the default org/key (`local` / `local-insecure-key`).
- All commands target localhost; remote flags are removed.
