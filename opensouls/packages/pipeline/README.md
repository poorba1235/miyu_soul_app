# @opensouls/pipeline

This repo holds the code to execute file system pipelines to send information up to the blueprint and organization stores.

Mostly you'll use this repo by running `bunx soul-engine install pipelines/markdown`
